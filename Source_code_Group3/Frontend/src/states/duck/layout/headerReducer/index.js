import * as types from './constants';
var initialState = {
    // toggleMenu:false,
};

var reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}
export default reducer;